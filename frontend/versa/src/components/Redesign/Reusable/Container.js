import styled from "styled-components";

const Container = styled.div`
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    padding: 0px;
    min-height: 60vh;
    margin: 8vh clamp(1em, 5vw, 3em);
`;

export default Container;
