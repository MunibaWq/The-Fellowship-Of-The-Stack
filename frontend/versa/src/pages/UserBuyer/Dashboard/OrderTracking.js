import React from "react";
import styled from "styled-components";

const OrderTracking = () => {
    return (
        <Container>
            This is where I can see all the orders I have placed.
        </Container>
    );
};

export default OrderTracking;

const Container = styled.div`
    margin: 5em 2em;
`;
